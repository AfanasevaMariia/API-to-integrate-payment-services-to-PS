# Connection String
spring.datasource.url=jdbc:postgresql://localhost:5432/parsedMessage
spring.datasource.username=postgres
spring.datasource.password=1234
# Recreates the DB each time when project is launched.
# Good for testing, comment this in production
spring.jpa.hibernate.ddl-auto=create-drop
# For viewing exceptions in console
server.error.include-parsedMessage=always
# Other settings
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.show-sql=true
spring.jpa.open-in-view=false